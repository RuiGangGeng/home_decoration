<!--pages/fl/index.wxml 分类-->
<view class="container">
 <view class="fl_bg">
  <navigator wx:for="{{categories}}" wx:key="index" url='/pages/content/detail?id={{item.id}}'>
   <image src="{{item.thumb_image}}"></image>
   <view>{{item.name}}</view>
  </navigator>
 </view>
</view>